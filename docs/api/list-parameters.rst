.. _api_reference_list_parameters:

Listing Available Parameter Sets
================================

The signatures below reflect ``tabulate``'s effect on the return value:
with ``tabulate=True`` (default), the function prints a table and returns
``None``; with ``tabulate=False``, it returns a list of parameter-set IDs
instead.

.. autofunction:: uqtestfuns.list_parameters
