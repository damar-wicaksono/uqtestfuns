.. _api_reference_list_functions:

Listing Available Functions
===========================

The signatures below reflect ``tabulate``'s effect on the return value:
with ``tabulate=True`` (default), the function prints a table and returns
``None``; with ``tabulate=False``, it returns a list of function names
instead.

.. autofunction:: uqtestfuns.list_functions