name: InvalidEvalNoFile
description: Specification with no corresponding Python module
tags: [metamodeling]

dimensions:
  input: variable
  output: 1

inputs:
  DefaultInput:
    description: "Default input"
    marginals:
      distribution: uniform
      parameters: [0.0, 1.0]